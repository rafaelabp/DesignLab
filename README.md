# DesignLab

Portfolio tecnico de Product Design construido com Next.js, App Router e TypeScript.

## Rodando localmente

```powershell
npm.cmd install
npm.cmd run dev
```

Depois acesse `http://localhost:3000`.
